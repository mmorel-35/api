version: v1
name: buf.build/istio/api
deps:
  - buf.build/googleapis/googleapis
  - buf.build/k8s/api
build:
  excludes:
    - common-protos
breaking:
  use:
    - WIRE_JSON
lint:
  use:
    - BASIC
  except:
    - FIELD_LOWER_SNAKE_CASE
    - PACKAGE_DIRECTORY_MATCH
  allow_comment_ignores: true
